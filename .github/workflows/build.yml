name: Build MinGW

on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            make
            gawk

      - name: Run tests
        shell: msys2 {0}
        run: make test

      - name: Compile
        shell: msys2 {0}
        run: make build

      - name: Install WiX Toolset
        run: dotnet tool install --global wix

      - name: Build MSI
        shell: msys2 {0}
        run: make msi VERSION=${{ github.ref_name }}

      - name: Extract changelog
        shell: msys2 {0}
        run: make release-notes > release.md

      - name: Create GitHub release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          bodyFile: release.md
          artifacts: switchy.exe,switchy.ini,switchy.msi
